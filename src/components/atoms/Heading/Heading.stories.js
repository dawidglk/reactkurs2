import React from 'react';
import { theme } from 'theme/mainTheme';
import Heading from './Heading';

export default {
  component: Heading,
  title: 'Heading',
};

export const HeadingCompon = () => (
  <Heading theme={theme} big>
    adfsd
  </Heading>
);
